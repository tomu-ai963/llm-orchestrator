{
  "name": "llm-council-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LLM Council Cloudflare Worker (openai / anthropic / grok)",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
